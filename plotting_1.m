x = 1:.1:10;
y = sin(x);
plot(x, y);
%plot(x, y, '*');
%plot(x, y, '--');
%plot(x, y, 'color',[0, 0.8,0.4]);
%plot(x, y), title('SINE AND COSINE GRAPH'), xlabel('X-axis'), ylabel('Y - axis'), grid on;
%g = cos(x)
%plot(x,y,x,g), title('SINE AND COSINE GRAPH'), legend('Sin(x)', 'Cos(x)');
%hold on
%g = cos(x);
%plot(x, g);
%t = asin(x);
%plot(x, t)